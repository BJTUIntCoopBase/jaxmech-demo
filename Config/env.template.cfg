# Machine-local environment configuration template.
# Recommended bootstrap: powershell -ExecutionPolicy Bypass -File .\Config\setup_demo_env.ps1
# On first run, the project will create Config/env.cfg from this template.
# Fill the values below for the current machine. Keep env.cfg local.

# WSL-side Python for JAX workflows (e.g. python3.11).
wsl_python =

# Windows-side Python for the Web UI (e.g. C:\Python39\python.exe).
windows_python_exe =

# Windows-side local model root (default: repo/StoredModels).
win_stored_root =

# WSL-side local model root (default: repo/StoredModels).
wsl_stored_root =
